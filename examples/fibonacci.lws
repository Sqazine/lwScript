fn fib(x)
{
    if(x<2)
        return x;
    else 
        return fib(x-1)+fib(x-2);
}

let start=time.clock();
let x=70;
let a=fib(x);
let end=time.clock();
io.println("fibonacci({}):{}",x,a);//9227465
io.println("{}",end-start);