fn fib(x)
{
    if(x==0)
        return 0;
    else if(x==1)
        return 1;
    else 
        return fib(x-1)+fib(x-2);
}

let fib=fn(x)
{
    if(x==0)
        return 0;
    else if(x==1)
        return 1;
    else 
        return fib(x-1)+fib(x-2);
};

let start=clock();
let a=fib(35);
let end=clock();
println("{}",a);//9227465
println("{}",end-start);