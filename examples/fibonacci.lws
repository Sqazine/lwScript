fn fib(x)
{
    if(x<2)
        return x;
    else 
        return fib(x-1)+fib(x-2);
}

let start=time.clock();
let a=fib(35);
let end=time.clock();
io.println("{}",a);//9227465
io.println("{}",end-start);